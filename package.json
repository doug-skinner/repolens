{
  "name": "repolens",
  "version": "0.8.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/index.tsx",
    "build": "bun run scripts/build.ts",
    "install-bin": "bun run build && sudo cp -f repolens /usr/local/bin/repolens",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "ink": "5.2.1",
    "ink-link": "4.1.0",
    "react": "18.3.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3",
    "@types/react": "^18.3",
    "ink-testing-library": "^4.0.0",
    "typescript": "^5.8"
  }
}
